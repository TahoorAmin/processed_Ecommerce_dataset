# processed_Ecommerce_dataset
Processed e-commerce dataset created using Pandas by combining Orders, Customers, and Products datasets with merge and concat. The project demonstrates data inspection, apply() for calculated columns, datetime operations for extracting date details, organizing the final dataset, and exporting the processed data as a CSV file.
